{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal surface. The shell adds no privileged commands of its own: everything the user sees is the web app, so nothing here needs to reach the filesystem or the shell.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
